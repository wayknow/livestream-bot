# 偏好优化方法全景图

> 基于之前数字人直播和文案风格微调经验，整理偏好优化方法对比。

---

## 一、偏好优化方法分类

### 按是否需要在线生成样本分类

```
┌─────────────────────────────────────────────────────────────┐
│  离线方法（Offline）          在线方法（Online）              │
│  ──────────────────           ─────────────────             │
│  用已有的成对数据直接优化        训练时实时生成答案，再用奖励反馈 │
│                                                              │
│  • DPO（已做）                • PPO（经典 RLHF）             │
│  • IPO（DPO改进版）            • GRPO（DeepSeek-R1用的）      │
│  • KTO（无需成对）             • REINFORCE Leave-One-Out      │
│  • ORPO（SFT+DPO二合一）                                     │
│  • SLiC / SimPO                                             │
└─────────────────────────────────────────────────────────────┘
```

### 按是否需要奖励模型（Reward Model）分类

- **需要 RM**：PPO、原始 RLHF
- **不需要 RM**：DPO、IPO、KTO、ORPO、GRPO

---

## 二、各方法详解

### 1. PPO（Proximal Policy Optimization）— 经典 RLHF

**原理**：强化学习的标准做法，需要 4 个模型：
- Policy 模型（正在学的）
- Reference 模型（SFT 基线）
- Reward 模型（给人打分的神经网络）
- Value 模型（Critic，预估未来收益）

**优点**：效果上限高，是 ChatGPT/Claude 早期版本的基础。
**缺点**：需要同时加载 4 个模型，显存爆炸；训练极不稳定。

**适用场景**：大厂有 A100/H100 集群时做完整 RLHF 流水线。
**Mac Mini**：❌ **跑不动 7B 模型**（4×7B=28B 参数）。

---

### 2. GRPO（Group Relative Policy Optimization）— DeepSeek-R1 的核心

**原理**：砍掉 Value 模型，用组内相对奖励替代。

**关键创新**：
- 对同一个问题，生成一组答案（4-8 个）
- 组内平均分作为 baseline
- 每个答案的相对优势 = 自己得分 - 组内平均分

**优点**：不需要 Value 模型，显存省 25%；天然消除题目难度偏差。

**适用场景**：数学推理、代码生成（有明确对错答案的任务）。
**Mac Mini**：⚠️ **3B 模型可以尝试**，7B 模型 24GB 内存紧张。

---

### 3. DPO（Direct Preference Optimization）— 已完成

**原理**：数学推导发现，PPO 里的 Reward 模型可以被解析消去，直接用成对的偏好数据优化 Policy。

**数据格式**：`{prompt, chosen, rejected}`

**优点**：简单、稳定、省显存。
**缺点**：
- 长度偏差：模型倾向于生成更长的文本
- 对数据质量敏感：如果 chosen/rejected 差距不够精细，容易过拟合
- 离线方法：无法利用训练过程中生成的新样本

**适用场景**：通用偏好对齐，资源有限时的首选。
**Mac Mini**：✅ **3B 轻松跑，7B 用 QLoRA 可以跑**。

---

### 4. IPO（Identity Preference Optimization）

**原理**：DPO 的改进版，解决 DPO 的过拟合和长度偏差问题。

**核心改进**：在 DPO loss 中加入正则项，让优化更温和。

**适用场景**：DPO 训崩了（loss 不收敛、输出越来越长、模式崩溃）时，换 IPO 试试。
**Mac Mini**：✅ 和 DPO 资源占用相同。

---

### 5. KTO（Kahneman-Tversky Optimization）

**原理**：DPO 需要成对数据，KTO 只需要二元标签（这条好/这条坏）。

**核心思想**：来自行为经济学的前景理论：
- 人类对"损失"的敏感度高于"收益"
- 模型应该同时学习"什么是好的"和"什么是坏的"
- 对"坏的"惩罚更重

**数据格式**：`{prompt, completion, label: true/false}`

**优点**：
- 数据构造成本低（不需要成对数据）
- 显存占用最低（只需一个模型）
- 适合有大量"单条标注"数据的场景

**适用场景**：资源有限，只有二元好/坏标签。
**Mac Mini**：✅ **最推荐**，7B QLoRA 非常舒服。

---

### 6. ORPO（Odds Ratio Preference Optimization）

**原理**：把 SFT 和 DPO 合二为一。

**核心改进**：在 SFT 的交叉熵 loss 里直接加入"偏好 odds ratio"项。

**优点**：省时间（一步训完），省显存（不需要 reference 模型）。
**缺点**：对超参数敏感，SFT 和偏好优化的平衡不好把握。

**适用场景**：想快速验证想法，不想先训 SFT 再训 DPO。
**Mac Mini**：✅ 省显存，推荐尝试。

---

### 7. SimPO / SimPPO / R-DPO 等（前沿变体）

**核心改进**：
- **SimPO**：去掉 reference 模型，用"生成长度归一化"解决 DPO 长度偏差
- **R-DPO（Robust DPO）**：对噪声偏好数据更鲁棒
- **Step-DPO**：针对推理任务，按"解题步骤"做偏好优化

**适用场景**：DPO 效果遇到瓶颈，想榨取最后 5% 性能时尝试。
**Mac Mini**：✅ 和 DPO 资源占用类似。

---

## 三、对比总表

| 方法 | 需要 RM | 需要 Reference | 在线/离线 | 显存占用 | 稳定性 | 适合任务 | Mac 24GB |
|------|---------|---------------|-----------|----------|--------|----------|----------|
| **PPO** | ✅ | ✅ | 在线 | 🔴 极高（4个模型） | 差 | 通用对话 | ❌ 不可 |
| **GRPO** | ⚠️ | ✅ | 在线 | 🟠 高（3个模型+组采样） | 中 | 数学/代码推理 | ⚠️ 3B可试 |
| **DPO** | ❌ | ✅ | 离线 | 🟡 中（2个模型） | 好 | 通用偏好对齐 | ✅ 7B QLoRA |
| **IPO** | ❌ | ✅ | 离线 | 🟡 中 | 更好 | DPO训崩时 | ✅ 7B QLoRA |
| **KTO** | ❌ | ❌ | 离线 | 🟢 低（1个模型） | 好 | 大量二元标注数据 | ✅ **最推荐** |
| **ORPO** | ❌ | ❌ | 离线 | 🟢 低（1个模型） | 中 | 快速一步训练 | ✅ 推荐 |
| **SimPO** | ❌ | ❌ | 离线 | 🟢 低 | 好 | 解决DPO长度偏差 | ✅ |

---

## 四、结合直播话术项目的推荐路线

### 当前状态
- ✅ SFT 已完成（Week 2）
- ✅ DPO 已完成（Week 3）
- ✅ IPO 已完成
- ✅ KTO 已完成

### 推荐迭代路线

```
第 1 轮：DPO v1（已完成）
  数据：人工好 vs 基座差
  目标：建立基本风格方向 ✅

第 2 轮：IPO 或 KTO（已完成）
  数据：优化后的偏好对
  目标：优化口头禅自然度、情绪递进层次 ✅

第 3 轮（可选）：ORPO
  跳过 SFT，直接用偏好数据一步训练
  适合：想验证"一步法"是否能 work

第 4 轮（探索）：GRPO
  如果未来做"直播话术 → 结构化卖点提取"或"用户问题 → 标准回答"
  适合：有明确对错边界的任务
```

---

## 五、一句话选择指南

| 你的情况 | 选什么 |
|---------|--------|
| 有 A100 集群，做通用对话产品 | PPO + RLHF（经典但重） |
| 做数学/代码/推理模型（如复现 R1） | **GRPO** |
| 资源有限，有成对偏好数据 | **DPO** → 崩了换 **IPO** |
| 资源有限，只有二元好/坏标签 | **KTO**（Mac 最友好） |
| 想一步搞定 SFT+偏好，快速验证 | **ORPO** |
| DPO 输出越来越长，控制不住 | **SimPO** 或 **IPO** |

---

## 六、本项目已实现的方法

| 方法 | 脚本 | 状态 | 结果 |
|------|------|------|------|
| DPO | `scripts/dpo_train.py` | ✅ | rewards/margins ↑ 9倍 |
| IPO | `scripts/ipo_train.py` | ✅ | Accuracy 23%，更稳定 |
| KTO | `scripts/kto_train.py` | ✅ | Neg Accuracy 100%，最省显存 |
| RLHF Demo | `scripts/ppo_train.py` | ✅ | 简化版 Demo |

---

**最后更新**：2026-08-31
**项目状态**：已实现 DPO、IPO、KTO 三种偏好优化方法
