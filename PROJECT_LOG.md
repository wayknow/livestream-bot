# 直播话术助手 — 项目决策日志

记录项目中的关键决策、原因和结果。

---

## 2026-08-27：项目初始化

### 决策 1：项目定位

**选择**：直播带货话术助手

**备选方案**：
- 客服话术生成器
- 营销文案助手
- 故事创作助手

**原因**：
- 用户有直播文案研究背景，效果直观
- 数据容易构造（品类+卖点+风格）
- 训完能直接用，验证快

---

### 决策 2：模型选择

**选择**：
- SFT：`Qwen/Qwen2.5-7B-Instruct`
- DPO：`Qwen/Qwen2.5-3B-Instruct`

**备选方案**：
- DeepSeek-R1-Distill-Qwen-7B（推理型）
- Qwen2.5-14B-Instruct（更大）

**原因**：
- Qwen 中文能力强，7B 在 24GB 上很舒服
- DPO 需同时加载 policy + reference，3B 刚好够
- 14B 可以但慢，先玩熟 7B

---

### 决策 3：训练框架

**选择**：
- SFT：mlx-lm（Apple Silicon 原生加速）
- DPO：TRL + PyTorch MPS

**备选方案**：
- 全用 TRL（transformers 生态）
- 全用 mlx-lm

**原因**：
- mlx-lm 对 Mac 优化最好，LoRA 训练快
- TRL 的 DPO 实现更成熟，mlx-lm 的 DPO 支持较新
- 混合使用取各自优势

---

### 决策 4：数据量

**选择**：
- SFT：200 条（50 手写 + 150 扩充）
- DPO：50-100 条

**备选方案**：
- 只写 50 条，不扩充
- 写 500+ 条

**原因**：
- 24GB 跑 600 iters 很快，200 条 × 3 epoch 合理
- 质量 > 数量，10 条精心设计的比 1000 条垃圾有效
- 扩充用模板替换，保持质量一致

---

### 决策 5：项目目录

**选择**：`/Users/xiaoxiao/work/finetuning/livestream-bot/`

**备选方案**：`~/ai-lab/livestream-bot/`

**原因**：
- 与用户其他项目（snapmark）在同一 work 目录下
- 路径更短，方便操作

---

## 2026-08-28：四周练手完成

### 决策 6：数据生成脚本设计

**选择**：模板替换法（10 品类 × 5 风格 + 自动扩充）

**备选方案**：
- 手动写 200 条
- 用 API 生成

**原因**：
- 模板替换可快速生成大量数据
- 保持风格一致性
- 可复用，便于迭代

**结果**：
- 成功生成 200 条 SFT + 100 条 DPO
- 首版有"扣1"重复问题，后修复

---

### 决策 7：DPO 训练设备

**选择**：CPU 训练

**备选方案**：MPS（Apple Silicon GPU）

**原因**：
- MPS 在 DPO 训练时内存不稳定（exit code 139）
- DPO 需同时加载 policy + reference 两个模型
- CPU 虽慢但稳定

**结果**：
- CPU 训练 538 秒完成
- 效果良好，rewards/margins 提升 9 倍

---

### 决策 8：互动指令插入频率

**选择**：每 4 句插入一次

**备选方案**：
- 每 2 句插入（首版）
- 每 6 句插入

**原因**：
- 每 2 句太频繁，导致"扣1"重复
- 每 4 句平衡互动频率和自然度
- 互动指令统一由 `generate_interaction` 管理

**结果**：
- 修复了"扣111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111"重复问题
- 生成质量显著提升

---

### 决策 9：生成随机性参数

**选择**：`temp=0.7, top_p=0.9`

**备选方案**：
- 默认参数（temp=0，确定性生成）
- temp=1.0（高随机）

**原因**：
- 默认参数每次生成一样，无实用价值
- temp=0.7 平衡随机性和质量
- top_p=0.9 从概率最高的 90% 词中采样，避免低质量词

**结果**：
- 10 次生成内容各不相同
- 质量稳定，适合实际使用

---

### 决策 10：模型融合方式

**选择**：mlx-lm fuse（LoRA 权重融合到基座）

**备选方案**：
- 每次推理时加载 adapter
- 转 GGUF 格式用 llama.cpp

**原因**：
- 融合后推理更简单，无需指定 adapter 路径
- 融合后模型可独立使用，不依赖训练框架
- 未来可转 GGUF 进一步优化

**结果**：
- 成功融合 15GB 完整模型
- 推理速度正常，效果良好

---

## 已决策（已完成）

- [x] Week 1：先写数据扩充脚本，再生成数据
- [x] Week 2：先测试基座效果，再训练对比
- [x] Week 3：DPO 用 CPU 训练（MPS 不稳定）
- [x] Week 4：用 mlx-lm fuse 融合模型

---

## 待决策

- [ ] 是否尝试 14B 模型？
- [ ] 是否部署为 API 服务？
- [ ] 是否转 GGUF 格式用 llama.cpp？
- [ ] 是否收集真实用户反馈？

---

**最后更新**：2026-08-28
**项目状态**：四周练手方案全部完成
